<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Peminjam - E6 Library</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body { font-family: 'Poppins', sans-serif; background-color: #f4f6f9; }
        .card { border: none; border-radius: 15px; box-shadow: 0 5px 15px rgba(0,0,0,0.05); }
    </style>
</head>
<body class="d-flex align-items-center justify-content-center" style="min-height: 100vh;">

    <div class="container" style="max-width: 500px;">
        <div class="card p-4">
            <div class="card-body">
                <div class="text-center mb-4">
                    <h4 class="fw-bold text-success"><i class="fas fa-user-edit me-2"></i>Edit Data Anggota</h4>
                    <p class="text-muted small">Perbarui informasi anggota perpustakaan</p>
                </div>

                <form action="/update-peminjam/<%= peminjam.id_peminjam %>" method="POST">
                    <div class="mb-3">
                        <label class="form-label text-muted small fw-bold">NAMA LENGKAP</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light border-end-0"><i class="fas fa-user text-muted"></i></span>
                            <input type="text" name="nama" class="form-control border-start-0 bg-light" value="<%= peminjam.nama %>" required>
                        </div>
                    </div>
                    <div class="mb-4">
                        <label class="form-label text-muted small fw-bold">NO. WHATSAPP/TELP</label>
                        <div class="input-group">
                            <span class="input-group-text bg-light border-end-0"><i class="fas fa-phone text-muted"></i></span>
                            <input type="text" name="nomor_telepon" class="form-control border-start-0 bg-light" value="<%= peminjam.nomor_telepon %>" required>
                        </div>
                    </div>
                    
                    <div class="d-grid gap-2">
                        <button type="submit" class="btn btn-success rounded-pill py-2 shadow-sm">Simpan Perubahan</button>
                        <a href="/peminjam" class="btn btn-light rounded-pill py-2 text-muted">Batal</a>
                    </div>
                </form>
            </div>
        </div>
    </div>

</body>
</html>